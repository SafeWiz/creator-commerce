**Label** — caption for a form control. Always give it `htmlFor` matching the input `id`.

```jsx
<Label htmlFor="price">Price (USD)</Label>
<Input id="price" />
```

14px Nunito Sans medium. Pass `disabled` to dim it alongside a disabled field.
